// Sprint 1: connection only. Domain models arrive in later sprints.
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
